
# # ═══════════════════════════════════════════════════════════════════════════
# #  HMS — Development Infrastructure
# #  Starts all supporting services. The Spring Boot app runs locally via Maven.
# # ═══════════════════════════════════════════════════════════════════════════

# services:

#   # ── PostgreSQL ─────────────────────────────────────────────────────────
#   postgres:
#     image: postgres:16-alpine
#     container_name: hms-postgres
#    # restart: unless-stopped
#     environment:
#       POSTGRES_DB: hms
#       POSTGRES_USER: hms_user
#       POSTGRES_PASSWORD: hms_dev_password
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#       - ./init-db:/docker-entrypoint-initdb.d   # optional: init scripts
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U hms_user -d hms"]
#       interval: 10s
#       timeout: 5s
#       retries: 5

#   # ── Redis ──────────────────────────────────────────────────────────────
#   # redis:
#   #   image: redis:7-alpine
#   #   container_name: hms-redis
#   #   restart: unless-stopped
#   #   command: redis-server --requirepass hms_redis_dev
#   #   ports:
#   #     - "6379:6379"
#   #   volumes:
#   #     - redis_data:/data
#   #   healthcheck:
#   #     test: ["CMD", "redis-cli", "ping"]
#   #     interval: 10s
#   #     timeout: 5s
#   #     retries: 5

#   # ── MinIO (S3-compatible object storage) ───────────────────────────────
#   # minio:
#   #   image: minio/minio:latest
#   #   container_name: hms-minio
#   #   restart: unless-stopped
#   #   command: server /data --console-address ":9001"
#   #   environment:
#   #     MINIO_ROOT_USER: minioadmin
#   #     MINIO_ROOT_PASSWORD: minioadmin123
#   #   ports:
#   #     - "9000:9000"   # API
#   #     - "9001:9001"   # Console UI
#   #   volumes:
#   #     - minio_data:/data
#   #   healthcheck:
#   #     test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
#   #     interval: 30s
#   #     timeout: 20s
#   #     retries: 3

#   # ── MailHog (dev email catcher — no real emails sent) ──────────────────
#   # mailhog:
#   #   image: mailhog/mailhog:latest
#   #   container_name: hms-mailhog
#   #   restart: unless-stopped
#   #   ports:
#   #     - "1025:1025"   # SMTP
#   #     - "8025:8025"   # Web UI

#   # ── Prometheus ─────────────────────────────────────────────────────────
#   # prometheus:
#   #   image: prom/prometheus:latest
#   #   container_name: hms-prometheus
#   #   restart: unless-stopped
#   #   command:
#   #     - "--config.file=/etc/prometheus/prometheus.yml"
#   #     - "--storage.tsdb.path=/prometheus"
#   #     - "--storage.tsdb.retention.time=7d"
#   #   ports:
#   #     - "9090:9090"
#   #   volumes:
#   #     - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro
#   #     - prometheus_data:/prometheus

#   # ── Grafana ────────────────────────────────────────────────────────────
#   # grafana:
#   #   image: grafana/grafana:latest
#   #   container_name: hms-grafana
#   #   restart: unless-stopped
#   #   environment:
#   #     GF_SECURITY_ADMIN_USER: admin
#   #     GF_SECURITY_ADMIN_PASSWORD: admin
#   #     GF_AUTH_ANONYMOUS_ENABLED: "false"
#   #   ports:
#   #     - "3000:3000"
#   #   volumes:
#   #     - grafana_data:/var/lib/grafana
#   #     - ./observability/grafana/provisioning:/etc/grafana/provisioning:ro
#   #   depends_on:
#   #     - prometheus

# # ═══════════════════════════════════════════════════════════════════════════
# volumes:
#   postgres_data:
#   # redis_data:
#   # minio_data:
#   # prometheus_data:
#   # grafana_data:


# # ═══════════════════════════════════════════════════════════════════════════
# the code below this commment is different from the above
#=======================================



# ═══════════════════════════════════════════════════════════════════════════
# HMS — Development Overrides
# ═══════════════════════════════════════════════════════════════════════════

services:

  postgres:
    ports:
      - "5432:5432"

  # redis:
  #   ports:
  #     - "6379:6379"

  # minio:
  #   ports:
  #     - "9000:9000"
  #     - "9001:9001"

  # mailhog:
  #   ports:
  #     - "1025:1025"
  #     - "8025:8025"

  # prometheus:
  #   ports:
  #     - "9090:9090"

  # grafana:
  #   ports:
  #     - "3000:3000"

  # ── Spring Boot App (Optional Dev Container) ──────────────────────────
  #
  # Uncomment if later you want backend fully containerized in dev
  #
  # hms-app:
  #   build:
  #     context: ./backend
  #
  #   container_name: hms-app-dev
  #
  #   environment:
  #     SPRING_PROFILES_ACTIVE: dev
  #
  #   ports:
  #     - "8080:8080"
  #
  #   volumes:
  #     - ./backend:/app
  #
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy